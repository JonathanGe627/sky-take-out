<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishMapper">

    <insert id="insertDish" useGeneratedKeys="true" keyProperty="id">
        insert into dish
            (name, category_id, price, image, description, status, create_time, update_time, create_user, update_user)
        values
            (#{name}, #{categoryId}, #{price}, #{image}, #{description}, #{status}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>

    <update id="updateDish">
        update dish
        set name=#{name},
            category_id=#{categoryId},
            price=#{price},
            image=#{image},
            description=#{description},
            status=#{status},
            update_time=#{updateTime},
            update_user=#{updateUser}
        where id = #{id}
    </update>

    <delete id="deleteDishes">
        delete from dish where id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>

    <select id="page" resultType="com.sky.vo.DishVO">
        select d.*, c.name category_name
        from dish d left join category c
        on d.category_id = c.id
        <where>
            <if test="name != null and name != ''">
                d.name like concat('%', #{name}, '%')
            </if>
            <if test="status != null">
                and d.status = #{status}
            </if>
            <if test="categoryId != null">
                and d.category_id = #{categoryId}
            </if>
        </where>
        order by d.create_time desc
    </select>

    <resultMap id="dishRM" type="com.sky.vo.DishVO">
        <id column="d_id" property="id"></id>
        <result column="d_name" property="name"></result>
        <result column="category_id" property="categoryId"></result>
        <result column="price" property="price"></result>
        <result column="image" property="image"></result>
        <result column="description" property="description"></result>
        <result column="status" property="status"></result>
        <result column="update_time" property="updateTime"></result>
        <result column="category_name" property="categoryName"></result>
        <collection property="flavors" ofType="com.sky.entity.DishFlavor">
            <id column="id" property="id"></id>
            <result column="dish_id" property="dishId"></result>
            <result column="name" property="name"></result>
            <result column="value" property="value"></result>
        </collection>
    </resultMap>
    <select id="getDishListByIds" resultMap="dishRM">
        select d.id d_id, d.name d_name, d.category_id, d.price, d.image, d.description, d.status, d.update_time,
        c.name category_name,
        df.*
        from dish d
        left join category c
        on d.category_id = c.id
        left join dish_flavor df
        on d.id = df.dish_id
        where d.id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </select>
    <select id="getDishListByCategoryId" resultMap="dishRM">
        select d.id d_id, d.name d_name, d.category_id, d.price, d.image, d.description, d.status, d.update_time,
        c.name category_name,
        df.*
        from dish d
        left join category c
        on d.category_id = c.id
        left join dish_flavor df
        on d.id = df.dish_id
        where d.category_id = #{categoryId} and d.status = #{status}
    </select>
    <select id="getDishItemBySetmealId" resultType="com.sky.vo.DishItemVO">
        select d.name, d.image, d.description, sd.copies
        from dish d
        left join setmeal_dish sd
        on d.id = sd.dish_id
        where sd.setmeal_id = #{setmealId}
    </select>
    <select id="countByMap" resultType="java.lang.Integer">
        select count(id) from dish
        <where>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
        </where>
    </select>
</mapper>